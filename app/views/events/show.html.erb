<h1><%= @event.user.first_name %> <%= @event.user.last_name %>'s Event</h1>
<h2>
  <%= @event.title %>
</h2>
<h3>
  Participants
</h3>
<div>
  <%= link_to  new_event_participant_path(@event) do %>
    Add a participant
    <i class="fa-solid fa-plus"></i>
  <% end %>
</div>
<% @event.participants.each do |participant| %>
  <p>
    <%= participant.first_name%> <%= participant.last_name %> <%= participant.email %>
  </p>
<% end %>


<div>
  <%= link_to  event_custom_path(@event) do %>
    Take a look at the Secret Santa picks and send an email to let them know who they have picked!
  <% end %>
</div>
